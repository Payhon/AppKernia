# akone

This package installs the native AppKernia `akone` executable for the current platform.

```sh
npm install --global @appkernia/akone
akone version --json
```

The native executable is shipped in an `os`/`cpu`-restricted optional dependency. Installation does not run a download script.
