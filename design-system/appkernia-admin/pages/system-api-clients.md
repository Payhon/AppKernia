# System API Clients Page Override

- Dense table with client ID, status, expiry, CIDR count and active secret count.
- Create/edit Drawer uses clear identity, expiry and CIDR sections.
- Secret rotation opens a one-time disclosure modal with copy control and mandatory saved acknowledgement.
- Permission assignment is an explicit scoped action; do not infer permissions from menu visibility.
