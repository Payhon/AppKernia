# Permission Catalog Page Overrides

- 只读目录按 module/resource 分组，提供关键词、module、resource、action、kind、status 筛选并写入 URL。
- 稳定权限码使用等宽文本展示并可复制；复制成功使用 `role=status`，不以 Toast 作为唯一信息载体。
- 窄屏保留权限码、资源和状态，次要列进入展开区；不展示或推断未授权敏感元数据。
