# Error and Offline Routes Override

- Explicit `/404` and `/offline` routes use semantic headings and concise recovery guidance.
- Recovery controls are real links/buttons with visible focus: retry the current location or return to Dashboard.
- Do not use decorative animation, countdown redirects, automatic write replay, or color as the only status indicator.
- Copy must switch between `zh-CN` and `en-US` without reload.
