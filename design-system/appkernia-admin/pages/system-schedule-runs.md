# Scheduled Job Run History Override

- Preserve App Shell and highlight the Schedules menu for the hidden deep link.
- Use a semantic `h1`, stable schedule UUID context, URL-backed status/page filters, error retry, and a helpful empty state.
- The table must remain keyboard reachable; on narrow screens use controlled internal horizontal scrolling without page overflow.
- Result payloads are formatted as bounded, read-only safe output; never render secrets or raw worker exceptions.
