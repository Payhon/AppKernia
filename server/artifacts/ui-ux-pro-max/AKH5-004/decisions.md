# Decisions

- 视频切换是播放器内部右上角的双图标组，不遮挡原生底部 controls；切换继续暂停视频。
- 语言入口使用地球 SVG，置于所有 H5 的 Header；原底部文字入口移除，链接仍使用显式 `lang` URL。
- “用户控制是否显示”落实为具备现有发行页更新权限的后台运营用户控制。推广开关同时控制 Header 下载入口和文章/单页文末推广；下载页本身保持独立公开状态。
- 推广标题、说明、按钮文字按 `zh-CN`/`en-US` 存储，允许空值以兼容存量配置并使用内置回退。关闭时服务端不输出推广 DOM。
