# ui-ux-pro-max output

Executed on 2026-08-05:

```text
python3 .codex/skills/ui-ux-pro-max/scripts/search.py "admin dictionary item creatable select color style presets" --design-system --project-name "AppKernia Dictionary Appearance Selects" --max-results 8
python3 .codex/skills/ui-ux-pro-max/scripts/search.py "combobox tags custom value keyboard color swatch form" --domain ux --max-results 8
python3 .codex/skills/ui-ux-pro-max/scripts/search.py "controlled creatable select form accessibility" --stack react --max-results 8
```

Relevant guidance retained:

- Use controlled React form components.
- Keep labels explicit and focus states visible.
- Make all functionality keyboard accessible.
- Never communicate meaning through color alone; pair swatches with names and literal values.
- Preserve loading and submission feedback.

The broad generated style recommendation was not used to replace the existing AppKernia design system; the page remains aligned with the repository master tokens and Ant Design controls.
