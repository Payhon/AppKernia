# Request

Improve the dictionary item editor so `color` and `css_class` are dropdown-selectable fields with useful defaults while still allowing administrators to enter and create custom values.

The existing tenant-override behavior, bilingual UI, and immutable built-in key/locale constraints must remain intact.
