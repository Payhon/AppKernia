# Request

Audit every Admin functional page where a Message/Alert appears above a table, card or other content surface. Correct the missing vertical separation shown in App Upgrade Center and apply one reusable rule without introducing duplicate spacing in dialogs, drawers or Ant Design `Space` layouts.

Acceptance targets: preserve the AppKernia Admin Master; use the existing spacing scale; cover desktop and mobile; keep page-level overflow at zero; add regression evidence and update the design-system contract.
