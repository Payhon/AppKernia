# ui-ux-pro-max output
## Design-system query

Query: `enterprise SaaS admin dictionary configuration notification templates accessibility`

- Recommended a flat, restrained enterprise interface with strong text contrast, simple hover feedback, visible focus, and no layout-shifting animation.
- Required checks include non-emoji icons, keyboard focus, reduced motion, and responsive behavior at 375/768/1024/1440 px.

## UX query

Query: `master detail dictionary forms conditional configuration delivery risk confirmation`

- Show an explicit confirmation before destructive or billable actions.
- Use visible form labels, inline errors, required indicators, loading state, and success/error feedback.
- Use appropriate email/tel/number input types and preserve browser autofill where safe.

## React query

Query: `responsive admin forms tables query state accessibility`

- Keep form controls managed through the existing RHF-controlled component pattern.
- Debounce or defer rapid search input.
- Associate every form label with its control and keep keyboard navigation available.
