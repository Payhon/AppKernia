# Screenshot index

## 2026-08-28 Article editor upgrade

- 代码级证据已完成：Admin build、typecheck、lint 和 component/unit tests 通过。
- 运行时截图暂未登记：`AK_E2E_ENV_MISSING`，缺少可用后端 fixture、Admin 账号和审计环境；不以静态组件、Mock 或旧页面截图冒充 Meta/内容 Tab、资源浏览器和媒体预览验收。

- 本轮已完成 TypeScript、测试与生产构建验证。
- 需要已迁移数据库、可用 Admin 账号和浏览器会话才能生成可信的 375/768/1440 双语运行时截图；未以静态组件或 Mock 冒充浏览器验收。

## 2026-08-28 测试反馈修复

- 已复核用户提供的两张修复前截图：文件选择器显示原始 i18n key；图文媒体卡只显示文件标题而没有缩略图。
- 修复后组件测试、生产构建和本地 4174 部署已通过，运行产物包含新双语文案和 `ak-content-gallery-preview`。
- 修复后真实账号截图尚未采集；待用户在当前浏览器会话强制刷新后复测，不将用户的修复前截图或 jsdom 结果冒充修复后视觉证据。

## 2026-08-28 Meta 排版反馈修复

- 用户附件登记为修复前证据：封面预览与四个开关挤在同一行，且关闭状态没有可见状态文字。
- 修复后代码将封面与发布选项拆为两个视觉行，四个选项响应式换行；字段名常驻，Switch 内显示本地化“是/否”。
- 修复后真实账号截图待当前本地部署完成后由登录会话复测，不以用户修复前截图或 jsdom 结果替代浏览器视觉验收。

## 2026-08-28 文件选择器缩略图反馈修复

- 用户附件登记为修复前证据：左侧列表仅显示 IMG 类型方块和横向文件名，不能在选择前辨识图片内容。
- 修复后实现为 112×72 图片缩略图优先、文件名在下的纵向单元格，并保留右侧大图预览。
- 修复后真实账号截图待本地部署与登录态复测；组件测试仅验证受保护 Blob 加载、DOM 层级和 URL 释放，不冒充视觉验收。

## 2026-08-28 文件选择器紧凑列表与时间筛选

- 本轮目标是把 112×72 缩略图压缩为 88×56、收紧表格内边距，并增加上传时间、文件大小和上传日期范围筛选。
- 修复后真实账号截图仍待登录态复测；自动化验证只登记组件结构、双语筛选控件、后端时间查询契约和生产构建，不冒充浏览器视觉验收。

## 2026-08-28 文件选择器 footer 操作分组

- 目标布局：上传按钮与策略提示在 footer 左侧，取消/选择在右侧同一行；窄屏允许上下分组。
- 修复后真实账号截图待登录态复测；组件测试只验证 DOM 分组、上传器不在 Modal body 以及默认操作按钮保留，不冒充视觉截图。

## 2026-08-28 文件选择器整行选择

- 目标交互：缩略图、文件名、上传时间、大小和扫描状态任意单元格点击均选中所在安全文件，同时保留 radio 指示。
- 修复后真实账号点击/键盘截图待登录态复测；组件测试只验证 metadata cell click、Enter/Space、`aria-selected` 和确认按钮解锁，不冒充视觉验收。

## 2026-08-28 文件选择器选中态与上传图标

- 用户附件登记为修复前证据：选中行使用近黑背景，文件名、上传时间和大小几乎不可读，footer 上传按钮缺少图标。
- 修复后真实登录态截图待本地部署后复测；自动化仅验证选中行状态类和上传 SVG 图标，不把 jsdom 结构断言冒充视觉验收。

## 2026-08-29 文件选择器可调窗口

- 本轮目标：左右 Splitter、预览关闭/展开、对话框最大化/还原、标题拖动和右下角缩放。
- 组件测试覆盖结构、状态切换和键盘路径；真实登录态 1440/900 与 768/375 拖拽、边界和视觉截图待本地部署后复测，不把 jsdom 或生产构建当作鼠标交互截图。

## 2026-08-29 文件选择器多视图与窗口操作组

- 本轮目标：右侧居中左箭头恢复预览、网格/紧凑表格/缩略图三种视图、浅灰已选提示及统一最大化/关闭按钮组。
- 已使用本地生产构建和受控 API fixture 执行真实 Chromium 1440×900 验证：`file-picker-thumbnail-zh-CN-1440.png`、`file-picker-grid-hover-zh-CN-1440.png`、`file-picker-table-zh-CN-1440.png`、`file-picker-preview-collapsed-zh-CN-1440.png`。
- `axe-results.json` 记录选择器范围 serious/critical 为 0，紧凑表格实测行高 24px，预览恢复箭头宽 24px且相对浏览区垂直偏移 0；表格截图同时登记浅灰已选提示、受保护图片预览和右上角等高按钮组。
- 以上是浏览器 fixture 视觉/交互证据，不冒充真实账号与对象存储联调；`en-US` 长文件名、暗色模式、768/375 和实际用户上传文件仍待登录态复测。

## 2026-08-29 全局下拉与共享可缩放对话框

- `file-picker-select-menu-zh-CN.png`：文件类型 Select 弹层局部截图；自动化读取 active/selected 背景 `rgb(220, 234, 255)`、文字 `rgb(23, 62, 122)`。`file-picker-select-open-zh-CN-1440.png` 同步登记所在页面上下文。
- `file-picker-resize-active-zh-CN-1440.png`：鼠标按住右下角弧线并拖动时的状态；弧线为焦点蓝，窗口从 1120×720 实际调整到 1160×744。
- 同一浏览器运行确认左侧最后一个表头右上圆角为 0px，选择器范围 axe serious/critical 为 0。证据使用受控 API fixture，不替代真实对象存储联调。
