# Request

Build the AppKernia App upgrade center while keeping the System mobile-release compatibility route on the same version fact source.

Required capabilities: selected App context; URL-backed query/type/platform/state/pagination filters; native and WGT create menus; bilingual title/content; internal file or HTTPS source; store relations; draft edit/delete; atomic publish/unpublish; frozen history; partial-online status; permission gates and responsive bilingual layouts.

Constraints: React + TanStack Query + RHF/Zod + Ant Design; preserve the existing AppKernia Admin Master design system and never add a second release fact source.
