# Request

Refine the existing AppKernia Rspress documentation site and republish it:

- unify the documentation identity with `apps/ak-admin/public/brand`;
- center and widen the three-column documentation layout on 1920 px screens;
- replace the generated Hero illustration with freshly captured Admin and iOS
  simulator product evidence;
- replace double-framed feature cards with accessible single-border link cards;
- expand the homepage and the Guide, Concepts, Server API, Mobile Components,
  and Community entry pages in both `zh-CN` and `en-US`;
- preserve existing routes, search, locale switching, dark mode, edit links,
  and GitHub Pages base-path behavior.

Required review widths are 375, 768, 1024, 1440, and 1920 px. Product claims
must distinguish local Docker, iOS simulator, compile-only, and unverified
physical-device/platform evidence.
