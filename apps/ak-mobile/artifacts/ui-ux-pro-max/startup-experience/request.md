# Request

Implement the screenshot-referenced first-install privacy gate and a versioned image-only onboarding flow for uni-app x on Android, iOS and HarmonyOS. Privacy must work offline before network/sensitive initialization; onboarding has no skip and completes only after every image is seen.
