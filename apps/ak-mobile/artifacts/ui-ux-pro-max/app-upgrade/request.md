# Request

Implement an accessible uni-app x native upgrade dialog for Android, iOS and HarmonyOS. It must cover optional/mandatory upgrades, app-store handoff, Android APK progress/install, retry and cancellation, with complete `zh-CN` and `en-US` content.
