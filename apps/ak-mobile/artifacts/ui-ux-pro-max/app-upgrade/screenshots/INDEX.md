# Screenshot Index

No screenshot is claimed by the code/static/compile checks in this delivery.

Required native evidence:

- Android 360×800: optional update, download progress, retry and APK installer.
- iOS 390×844: mandatory update and App Store return behavior.
- HarmonyOS 430×932: external-link fallback and mandatory return behavior.
- Each visual state in both `zh-CN` and `en-US`.
