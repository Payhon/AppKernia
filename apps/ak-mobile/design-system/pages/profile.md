# Profile override
- Use a compact identity header followed by grouped setting rows.
- Place destructive logout as a separate outlined red action below regular settings.
- The Profile tab is active; avatar is decorative unless an explicit edit affordance is shown.
