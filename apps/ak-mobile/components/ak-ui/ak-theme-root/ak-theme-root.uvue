<template><view :class="[themeClass, 'ak-theme-root', statusTone == 'startup' ? 'ak-theme-root--startup' : '']"><slot/></view></template>
<script lang="uts">import{themeState}from'../../../src/core/theme/theme-store.uts';export default{props:{statusTone:{type:String,default:'default'}},computed:{themeClass(){return themeState.isDark?'ak-theme-dark':'ak-theme-light'}}}</script>
<style lang="scss">
.ak-theme-root {
  height:100%;
  flex:1;
  padding-top:var(--ak-safe-top);
  box-sizing:border-box;
  background:var(--ak-page);
}
.ak-theme-root--startup { background:var(--ak-startup-backdrop); }
</style>
