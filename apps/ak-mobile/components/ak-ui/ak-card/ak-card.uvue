<template><view class="ak-card"><slot /></view></template>
<style lang="scss">.ak-card { background:var(--ak-surface); border:1px solid var(--ak-border); border-radius:var(--ak-radius-card); padding:var(--ak-space-4); box-sizing:border-box; }</style>
