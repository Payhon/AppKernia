# ui-ux-pro-max Output

## Commands executed

```bash
python3 .codex/skills/ui-ux-pro-max/scripts/search.py "SaaS admin authentication login language icon dropdown minimal accessible" --design-system -p "AppKernia Admin Login Locale Menu"
python3 .codex/skills/ui-ux-pro-max/scripts/search.py "language selector dropdown keyboard focus accessibility authentication" --domain ux -n 8
python3 .codex/skills/ui-ux-pro-max/scripts/search.py "controlled locale dropdown menu state async preference" --stack react
python3 .codex/skills/ui-ux-pro-max/scripts/search.py "icon button dropdown menu aria selected focus mobile" --domain web -n 8
```

All four commands exited 0.

## Relevant output applied

- Use a semantic icon button with a translated accessible name; decorative icon graphics remain hidden from assistive technology.
- Preserve visible focus and complete keyboard access for the trigger and menu.
- Use the existing global locale context and handle asynchronous authenticated preference failures without unhandled promises.
- Keep the popup text at WCAG AA contrast, expose the current language with a selected menu state, and retain a 40px touch target.
- Validate responsive behavior at authentication-page breakpoints with no horizontal overflow.

## Existing design system overrides

The generated generic suggestions for gold CTA, external Inter loading, animated patterns, and marketing sections were not applied because the repository Master explicitly requires local system fonts, ink controls, restrained authentication surfaces, and no marketing Hero content.
