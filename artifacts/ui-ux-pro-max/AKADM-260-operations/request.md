# AKADM-260 UI Skill Request

Real project-local `ui-ux-pro-max` commands executed on 2026-08-03:

1. `enterprise security access control operations health dashboard calm professional accessible --design-system --persist -p "AppKernia Admin Operations"`
2. `security block rules redacted subjects impact confirmation responsive data table --design-system --persist -p "AppKernia Admin Operations" --page "access-rules"`
3. `service health dependency runtime queue status diagnostic summary --design-system --persist -p "AppKernia Admin Operations" --page "service-health"`
4. `destructive access rule impact confirmation redaction safe diagnostics accessibility --domain ux`
5. `health status summary dependency latency queue cards refresh responsive --stack react`

Required outcomes: server-redacted subjects, explicit impact confirmation, reversible status controls, dependency/queue health without connection strings, safe diagnostic copy, bilingual responsive and accessible states.
