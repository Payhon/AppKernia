# AKADM-200 UI Skill Request

Date: 2026-08-03

Build the production Admin UX for system configuration and localized dictionaries. The configuration page needs module/group filters, typed values, optimistic version feedback, explicit public/secret semantics, and a secret workflow that never echoes plaintext. The dictionary page needs a responsive master-detail layout, locale fallback visibility, locked system records, sortable items, and complete loading/empty/error/permission states. Target React + Ant Design at 375/768/1024/1440 with zh-CN and en-US.

Queries executed:

1. `enterprise configuration secrets dictionary admin data dense accessible --design-system`
2. `secret configuration rotate replace keep unchanged destructive confirmation never reveal --domain ux`
3. `dictionary master detail localization fallback locked system records admin --domain ux`
4. `responsive master detail settings tables drawers forms accessibility --stack react`
5. `responsive admin table horizontal scroll mobile master detail --domain web`
