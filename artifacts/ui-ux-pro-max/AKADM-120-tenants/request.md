# AKADM-120 Tenant Management Request

为 AppKernia Admin 设计并实现真实租户列表/详情、成员管理和工作区切换。必须覆盖 `multi_tenant` feature flag、URL 状态恢复、逐动作权限、跨租户隔离、切换后 tenant-scoped Query Cache 清理、Auth Context 重取、双语、响应式、键盘和错误恢复。
