# AKADM-250 UI Skill Request

Real project command set:

1. `enterprise API client webhook secrets permissions delivery dashboard secure accessible --design-system --persist -p "AppKernia Admin Integrations"`
2. `one time secret disclosure copy confirmation --domain ux`
3. `webhook endpoint SSRF validation delivery history retry --domain ux`
4. `data table drawer secret warning responsive accessibility --domain ux`
5. `React Ant Design permissions forms tables drawer --stack react`

Required outcomes: one-time secret disclosure, CIDR/expiry/permission management, SSRF guidance, signed test delivery, safe delivery history, dual-language responsive and accessible states.
